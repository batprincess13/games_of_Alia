function toolboxQuestion(i){const qs=[()=>numeric("A toolbox has 2 rows of 4 nails. How many nails?",8,"2 groups of 4."),()=>numeric("3 boards need 5 screws each. How many screws?",15,"3 groups of 5."),()=>numeric("A board is 4 cm by 3 cm. What is its area?",12,"Length × width."),()=>mc("Two rails that never meet are…","parallel",["perpendicular","intersecting","a ray"],"They stay the same distance apart."),()=>numeric("What is the value of the 5 in 3,542?",500,"The 5 is in the hundreds place."),()=>numeric("Round 1,648 to the nearest hundred.",1600,"Check the tens digit."),()=>mc("Which is greater?","4,210",["4,120","equal","4,012"],"Compare place values."),()=>numeric("You have ₱300 and earn ₱125. How much now?",425,"Add."),()=>numeric("You have 900 screws and use 250. How many remain?",650,"Subtract."),()=>numeric("6 brackets with 4 screws each need how many screws?",24,"6 groups of 4.")];return qs[i]()}
function startPlayer(){const name=$("playerName").value.trim();if(!name)return;const key=name.toLowerCase().replace(/\s+/g," "),ps=loadProfiles();if(!ps[key])ps[key]=freshProfile(name);state.key=key;state.profile=ps[key];state.profile.portfolio=state.profile.portfolio||[];state.profile.seenQuestions=state.profile.seenQuestions||{};localStorage.setItem(PROFILE_KEY,JSON.stringify(ps));$("startScreen").classList.add("hidden");$("game").classList.remove("hidden");if(!state.profile.toolboxDone)showToolbox();else newJob();renderAll()}
function renderAll(){renderHeader();renderTiers();renderSkills();renderWorkshop();renderMaterials()}
function renderHeader(){const t=activeTier(),q=currentQuarter();$("playerPill").textContent="👷 "+state.profile.name;$("tierPill").textContent=TIERS[t].icon+" "+TIERS[t].name;$("quarterPill").textContent="📘 Quarter "+q;$("cashStat").textContent=peso(state.profile.cash);$("jobsStat").textContent=state.profile.jobs;$("masteredStat").textContent=masteredCount()+"/"+SKILLS.length;$("evidenceStat").textContent=totalEvidence()}
function renderTiers(){const cur=activeTier();$("bizPath").innerHTML=TIERS.map((t,i)=>{const unlocked=tierUnlocked(i),done=i<cur||(i===0&&state.profile.toolboxDone);return `<div class="tier ${i===cur?"current":done?"done":"locked"}"><strong>${t.icon} ${t.name}</strong><div class="small muted">${t.desc}</div><div class="small">${done?"✓ Completed":unlocked?"● Current":"🔒 Locked by math mastery"}</div></div>`}).join("")}
function renderSkills(){const q=currentQuarter(),cur=state.q?.skillId||chooseSkill().id;$("skillList").innerHTML=SKILLS.filter(s=>s.q===q).map(s=>{const st=getStat(s.id),a=Math.round(accuracy(s.id)*100),locked=!prereqsMet(s.id)&&!st.mastered,cov=Math.round(componentCoverage(s.id)*100);return `<div class="skill ${s.id===cur?"active":""} ${st.mastered?"mastered":""}"><strong>${s.n}</strong> <span class="badge">${st.mastered?"MASTERED":locked?"UP NEXT":"LEARNING"}</span><small>${st.possible?`${a}% accuracy • ${cov}% component coverage`:"Teaching not yet started"}</small></div>`}).join("")}
function renderWorkshop(){const sk=state.q?S[state.q.skillId]:chooseSkill(),st=getStat(sk.id),a=Math.round(accuracy(sk.id)*100),cov=Math.round(componentCoverage(sk.id)*100);$("focusSkill").textContent=sk.n;$("focusMastery").textContent=(st.possible?a:0)+"%";$("focusBar").style.width=(st.possible?a:0)+"%";$("focusEvidence").textContent=`${cov}% of competency components independently demonstrated`;$("adaptiveNote").textContent=st.mastered?"Mastered. This skill will return naturally for retention.":"Builder Quest stays on this curriculum competency, targets its weakest component, teaches first, then removes support."}
function renderMaterials(){const tools=state.profile.tools||[],inv=state.profile.inventory||{};$("materials").innerHTML=[...tools.slice(-4).map(t=>`<div class="material">${t.icon} <strong>${t.name}</strong></div>`),`<div class="material">🪵 Wood <strong>${inv.wood||0}</strong></div>`,`<div class="material">🔩 Screws <strong>${inv.screws||0}</strong></div>`].join("")}
function showToolbox(){$("jobArea").classList.add("hidden");$("completionCard").classList.add("hidden");$("toolboxIntro").classList.remove("hidden");const i=state.profile.toolboxIndex||0;if(i>=10){finishToolbox();return}state.q={...toolboxQuestion(i),toolbox:true};state.answered=false;state.hinted=false;const tool=TOOLBOX[i];$("toolboxIntro").innerHTML=`<div class="jobHeader"><div><span class="badge">TOOLBOX TUTORIAL ${i+1}/10</span><h2>Earn: ${tool[0]} ${tool[1]}</h2><p class="muted">Starter diagnostic only. It never certifies curriculum mastery.</p></div><div class="jobIcon">${tool[0]}</div></div><div class="step"><div class="context">BASIC CHECK</div><div class="q">${state.q.text}</div><div id="tbAnswer"></div><div id="tbFeedback" class="feedback"></div><div class="row"><button id="tbHint" class="secondary">Hint</button><button id="tbNext" class="gold hidden">Next tool</button></div></div>`;renderInlineAnswer("tbAnswer",state.q,submitToolbox);$("tbHint").onclick=()=>{$("tbFeedback").textContent="Hint: "+state.q.hint;$("tbFeedback").className="feedback hintText";state.hinted=true};$("tbNext").onclick=()=>{state.profile.toolboxIndex++;saveProfiles();showToolbox();renderAll()}}
function renderInlineAnswer(id,q,handler){const el=$(id);if(q.type==="choice"){el.innerHTML=`<div class="choices">${q.options.map(o=>`<button class="choice" data-v="${String(o).replace(/"/g,"&quot;")}">${o}</button>`).join("")}</div>`;el.querySelectorAll("button").forEach(b=>b.onclick=()=>handler(b.dataset.v))}else{el.innerHTML=`<div class="answerRow"><input id="${id}Input"><button id="${id}Check">Check</button></div>`;const inp=$(id+"Input");$(id+"Check").onclick=()=>handler(inp.value.trim());inp.addEventListener("keydown",e=>{if(e.key==="Enter")handler(inp.value.trim())})}}
function submitToolbox(v){if(state.answered)return;const correct=String(v).trim().toLowerCase()===String(state.q.answer).trim().toLowerCase();state.answered=true;state.profile.toolboxDiagnostic=state.profile.toolboxDiagnostic||[];state.profile.toolboxDiagnostic.push({index:state.profile.toolboxIndex,correct,hinted:state.hinted});if(correct){const t=TOOLBOX[state.profile.toolboxIndex];state.profile.tools.push({icon:t[0],name:t[1]});$("tbFeedback").textContent=`Correct. ${t[1]} added.`;$("tbFeedback").className="feedback good"}else{$("tbFeedback").textContent=`Correct answer: ${state.q.answer}. Diagnostic only, so this does not lower mastery.`;$("tbFeedback").className="feedback bad"}$("tbNext").classList.remove("hidden");saveProfiles();renderAll()}
function finishToolbox(){state.profile.toolboxDone=true;state.profile.tier=1;state.profile.cash+=100;saveProfiles();$("toolboxIntro").classList.add("hidden");$("completionCard").classList.remove("hidden");$("completionCard").innerHTML=`<h2>🧰 Toolbox Ready</h2><p>Starter tools earned. Your first paid job now teaches the first Grade 3 competency.</p><button id="firstJob">Accept First House Call</button>`;$("firstJob").onclick=()=>{$("completionCard").classList.add("hidden");newJob();renderAll()};renderAll()}
function learningPhase(id){const st=getStat(id);const recent=st.recent.slice(-2);if(st.learning.guided<2)return "guided";if(recent.length===2&&recent.every(x=>!x))return "guided";return "independent"}
function currentCurriculumSkill(){return chooseSkill()}
function newJob(){if(!state.profile.toolboxDone){showToolbox();return}$("toolboxIntro").classList.add("hidden");$("completionCard").classList.add("hidden");$("jobArea").classList.remove("hidden");const tier=activeTier(),pool=JOBS[TIERS[tier].id]||JOBS.calls,meta=pick(pool),primary=currentCurriculumSkill(),phase=learningPhase(primary.id),mastered=SKILLS.filter(s=>getStat(s.id).mastered),steps=tier===1?3:tier===2?4:5,questions=[];for(let i=0;i<steps;i++){let id=primary.id,lp=phase;if(phase==="independent"&&mastered.length&&i===steps-1){id=pick(mastered).id;lp="review"}const ctx=lp==="guided"?"applied":pick(["direct","applied","reasoning"]),q=genQuestion(id,ctx);questions.push({...q,skillId:id,learningPhase:lp})}state.job={title:meta[0],desc:meta[1],icon:meta[2],tier,questions,index:0,earned:0,primary:primary.id,phase};$("jobType").textContent=TIERS[tier].name.toUpperCase();$("jobTitle").textContent=meta[0];$("jobDesc").textContent=meta[1];$("jobIcon").textContent=meta[2];showJobQuestion()}
function showJobQuestion(){const j=state.job;if(j.index>=j.questions.length){finishJob();return}state.q=j.questions[j.index];state.answered=false;state.hinted=false;const q=state.q,lesson=LESSONS[q.skillId];$("contextLabel").textContent=(q.learningPhase==="guided"?"FOREMAN TRAINING":q.learningPhase==="independent"?"YOUR TURN":"SKILL REVIEW")+" • "+S[q.skillId].n.toUpperCase();$("questionVisual").innerHTML=(q.learningPhase==="guided"?`<div class="notice"><strong>Learn this for the job:</strong> ${lesson.teach}<br><br><strong>Method:</strong> ${lesson.coach}</div>`:"")+(q.visual||"");$("questionText").textContent=q.text;$("feedback").textContent="";$("nextBtn").classList.add("hidden");$("hintBtn").disabled=false;$("jobProgress").style.width=(j.index/j.questions.length*100)+"%";$("jobMoney").textContent=peso(j.earned);if(q.type==="choice"){$("inputAnswer").classList.add("hidden");$("choices").classList.remove("hidden");$("choices").innerHTML=q.options.map(o=>`<button class="choice" data-v="${String(o).replace(/"/g,"&quot;")}">${o}</button>`).join("");$("choices").querySelectorAll("button").forEach(b=>b.onclick=()=>submitJob(b.dataset.v))}else{$("choices").classList.add("hidden");$("inputAnswer").classList.remove("hidden");$("answerInput").value="";$("checkBtn").disabled=false}renderAll()}
function submitJob(v){if(state.answered)return;const q=state.q,correct=String(v).trim().toLowerCase()===String(q.answer).trim().toLowerCase();state.answered=true;if(q.learningPhase==="guided")recordEvidence(q.skillId,correct,q.context,state.hinted,true,q.component);else recordEvidence(q.skillId,correct,q.context||"applied",state.hinted,false,q.component);$("hintBtn").disabled=true;$("checkBtn").disabled=true;$("choices").querySelectorAll("button").forEach(b=>b.disabled=true);if(correct){const pay=q.learningPhase==="guided"?15:q.context==="reasoning"?35:25;state.job.earned+=pay;const m=pick(["wood","screws","brackets","paint"]);state.profile.inventory[m]=(state.profile.inventory[m]||0)+1;$("feedback").textContent=q.learningPhase==="guided"?"Correct. You used the taught method. Guidance will fade when you are ready.":"Correct. This independent decision is now part of the mastery evidence.";$("feedback").className="feedback good"}else{$("feedback").textContent=q.learningPhase==="guided"?`Not yet. Correct answer: ${q.answer}. The Foreman will keep teaching this component.`:`Not yet. Correct answer: ${q.answer}. The game will target this component again instead of moving on.`;$("feedback").className="feedback bad"}$("nextBtn").classList.remove("hidden");saveProfiles();renderAll()}
$("checkBtn").onclick=()=>submitJob($("answerInput").value);$("answerInput").addEventListener("keydown",e=>{if(e.key!=="Enter")return;e.preventDefault();if(!state.answered)submitJob($("answerInput").value);else nextJobStep()});$("nextBtn").onclick=nextJobStep;function nextJobStep(){state.job.index++;showJobQuestion()}$("hintBtn").onclick=()=>{state.hinted=true;$("feedback").textContent="Hint: "+state.q.hint;$("feedback").className="feedback hintText"};
function finishJob(){const j=state.job,base=[0,900,2400,5200,9000][j.tier]||900,materialCost=Math.max(250,Math.round(base*.45)),feeSets={1:[450,700,1000],2:[1000,1500,2000],3:[1800,2500,3000],4:[2500,3000,4000]},fees=feeSets[j.tier]||[450,700,1000];$("jobArea").classList.add("hidden");$("completionCard").classList.remove("hidden");$("completionCard").innerHTML=`<span class="badge">JOB READY TO QUOTE</span><h2>${j.icon} ${j.title}</h2><p>Choose the labor/service fee for the completed work.</p><div class="materials"><div class="material"><small>Materials</small><strong>${peso(materialCost)}</strong></div><div class="material"><small>Math earned</small><strong>${peso(j.earned)}</strong></div></div><div class="choices" id="feeChoices">${fees.map((f,i)=>`<button class="choice" data-fee="${f}">${peso(f)} — ${i===0?"basic":i===1?"standard":"premium"}</button>`).join("")}</div>`;$("feeChoices").querySelectorAll("button").forEach(b=>b.onclick=()=>finalizeJob(materialCost,Number(b.dataset.fee)))}
function finalizeJob(materialCost,serviceFee){const j=state.job,quote=materialCost+serviceFee,st=getStat(j.primary);state.profile.cash+=serviceFee;state.profile.jobs++;state.profile.portfolio.push({title:j.title,tier:TIERS[j.tier].name,date:new Date().toISOString().slice(0,10),skill:S[j.primary].n,materials:materialCost,serviceFee,quote,profit:serviceFee,mastered:st.mastered,componentCoverage:Math.round(componentCoverage(j.primary)*100)});saveProfiles();$("completionCard").innerHTML=`<h2>Contract Complete</h2><div class="materials"><div class="material"><small>Materials</small><strong>${peso(materialCost)}</strong></div><div class="material"><small>Service fee</small><strong>${peso(serviceFee)}</strong></div><div class="material"><small>Customer quote</small><strong>${peso(quote)}</strong></div><div class="material"><small>Profit</small><strong>${peso(serviceFee)}</strong></div></div><div class="notice">Saved to Builder Portfolio. ${st.mastered?"Competency mastered. It now becomes a working skill in later jobs.":`Current competency remains active. Independent component coverage: ${Math.round(componentCoverage(j.primary)*100)}%.`}</div><button id="anotherJob">Accept Next Contract</button>`;$("anotherJob").onclick=()=>{$("completionCard").classList.add("hidden");newJob();renderAll()};renderAll()}
